import { useEffect } from "react"
import { useParams } from "react-router-dom"

export default function Details() {
    const {id} = useParams()
  
    useEffect(() => {
      
    }, [third])
    
  
  
    return (
    <div>Details</div>
  )
}
